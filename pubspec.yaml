name: aarogya_mitra
description: Marathi Voice Health Assistant

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.0
  sqflite: ^2.0.0+4
  path_provider: ^2.0.0
  http: ^0.13.0
  shared_preferences: ^2.0.0
  flutter_tts: ^4.0.0
  cupertino_icons: ^1.0.2
  record: ^6.1.2
  path: ^1.9.0
  permission_handler: ^12.0.1
  flutter_dotenv: ^6.0.0
  intl: ^0.20.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true